// scripts/03_grant_deleter.ts
// import { ethers } from "hardhat"; import * as dotenv from "dotenv"; dotenv.config();
// async function main() {
//     const nft = await ethers.getContractAt("SurveyNFT", process.env.NFT_ADDR!);
//     const deleter = process.env.DELETER_ADDR!; // dein Admin-Wallet
//     const tx = await nft.grantDeleter(deleter);
//     await tx.wait(); console.log("DELETER_ROLE ->", deleter);
// }
// main();
